// Prerender the entire site to static HTML. Content updates are published by a
// Prismic webhook hitting a Cloudflare Pages deploy hook, which rebuilds.
export const prerender = true
export const trailingSlash = 'never'
